<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.store.goguma.repository.FreeBoardRepository">

	<select id="findAllFree">
		select * from free_board where delete_yn = 'N'
	</select>

	<!--게시글 좋아요 개수 카운트 -->
	<select id="countRecommendation">
		select fb.id as id,
		fb.title as title,
		fb.content as content,
		fb.u_id as u_id,
		fb.file as file,
		fb.create_at as created_at,
		fb.update_at as updated_at,
		fb.delete_at as deleted_at,
		count(fr.free_board_id) as good_count
		from free_board as fb
		left join free_board_recommendation fr on fb.id = fr.free_board_id and
		fr.good_yn = 'Y'
		where fb.delete_yn = 'N'
		group by fb.id, fb.title, fb.content, fb.u_id, fb.file, fb.create_at,
		fb.update_at, fb.delete_at
	</select>

	<!-- 게시글 좋아요 추가 -->
	<insert id="addRecommendation">
		insert into free_board_recommendation
		(`free_board_id`,`u_id`) values (#{freeBoardId},#{uId})
	</insert>

	<!-- 게시글 좋아요 취소 -->
	<update id="deleteRecommendation">
		update free_board_recommendation set delete_yn = 'N'
		where free_board_id = #{freeBoardId} and u_id = #{uId}
	</update>

	<!-- 게시글 조회수 확인 -->
	<select id="getFreeView">
		select view from free_board_view where free_board_id
		= #{freeBoardId}
	</select>

	<!-- 게시글 조회수 증가 -->
	<update id="plusFreeView">
		UPDATE free_board_view
		SET view = view + 1
		WHERE
		free_board_id = #{freeBoardId}
	</update>
	
	<insert id="addFreeView">
		
	</insert>
	
	<select id="selectByFid" resultType="com.store.goguma.freeboard.dto.FreeBoardDetailDTO">
		SELECT fd.*,
			u.`file` as `userFile`,
			v.`view`,
			r.good 
		FROM free_board as fd
			join `user` as u 
				on fd.u_id = u.u_id
			join free_board_view as v 
				on fd.id = v.free_board_id
			join free_board_recommendation as r 
				on fd.id = r.free_board_id
		where `id`=#{fId}
	</select>
	
	<insert id="insertFreeBoard">
		insert into free_board (title,content,u_id,main_category,sub_category)
			values(#{title},#{content},#{uId}, #{mainCategory},#{subCategory});
	</insert>
</mapper>