<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.store.goguma.repository.UserRepository">

	<select id="selectByEmail" resultType="com.store.goguma.entity.User">
		select * from user where CONCAT(social, email) = #{userEmail}
	</select>

	<update id="updateUser">
		update `user` set `zip`=#{zip}, `address`=#{address}, `tel`=#{tel}, `file`=#{file},`update_at`=NOW()
			where `social`=#{social} and `email`=#{email}
	</update>
</mapper>